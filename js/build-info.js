/* Shared by the page, asset loader and service worker. Bump for each release. */
globalThis.RHW_BUILD = Object.freeze({
  revision: '2026-09-14-runtime-cleanup-1',
  repository: 'PhyteHQ/RHW',
  branch: 'main'
});
