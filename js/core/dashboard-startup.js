/* Dashboard startup. All owning modules are loaded first. */
restoreViewPreferences();
applyFeatureVisibility();
initEcoMode();
updateRoleSegments();
updateSortArrows();
updateMarketSortButtons();

refreshAll();
